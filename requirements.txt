streamlit
openai
python-dotenv
psutil
keyboard

# API server dependencies
fastapi
uvicorn[standard]
httpx

# RAG dependencies
fastembed
annoy

# NeMo Guardrails - install separately to avoid C++ build dependency (annoy):
#   pip install nemoguardrails --no-deps
#   pip install aiohttp aiohttp-retry dataclasses-json lark nest-asyncio prompt-toolkit simpleeval
nemoguardrails

# Test dependencies
hypothesis>=6.0
pytest>=7.0
pytest-asyncio>=0.21
